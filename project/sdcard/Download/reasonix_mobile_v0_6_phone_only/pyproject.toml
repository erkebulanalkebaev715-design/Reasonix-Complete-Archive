[project]
name = "reasonix-model"
version = "0.4.0"
description = "From-scratch mobile language model architecture research prototype"
requires-python = ">=3.10"
dependencies = ["torch>=2.1"]

[tool.setuptools]
packages = ["reasonix_model"]
