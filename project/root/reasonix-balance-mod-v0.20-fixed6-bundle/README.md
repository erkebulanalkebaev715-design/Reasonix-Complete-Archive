Reasonix Balance Mod v0.20 Fixed6

Install over the existing v0.20 tree. This fixes the strict-budget real DeepSeek transport by using one bounded non-stream request under hard-budget mode, while normal Reasonix remains streaming. Installer performs no provider/API call.
